{
  "name": "techtonic",
  "version": "1.0.0",
  "description": "Techtonic - Timer and Productivity App",
  "scripts": {
    "install:frontend": "npm install --prefix src/frontend",
    "install:backend": "npm install --prefix src/backend",
    "install:all": "npm run install:frontend && npm run install:backend",
    "build:frontend": "npm run build:all --prefix src/frontend",
    "build": "npm run install:frontend && npm run build:frontend && npm run install:backend",
    "start": "npm start --prefix src/backend",
    "dev:frontend": "npm run serve --prefix src/frontend",
    "dev:backend": "npm run dev --prefix src/backend"
  },
  "author": "",
  "license": "ISC"
}
